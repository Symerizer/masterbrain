<ul>
	{{#each user in controller}}
  		<li {{action "selectUser" user}}>{{#if user.online}}<span style="color:green;width:50px">  Online {{else}}<span style="color:red;width:50px"> Offline {{/if}}</span> : {{user.nickname}}</li>
	{{/each}}
</ul>

<br/>
<br/>

{{#if Edit}}
	<form {{action "updateUser" on="submit"}}>
		nickname : {{input name="nickname" value=UserEditParam.nickname}}<br/>
		{{input type="submit" value="save"}}
	</form>
{{/if}}